# Git Pull 快速指南

## 方式一：PyCharm GUI

1. 打开 PyCharm 中的 TextMamba3D 项目
2. 菜单 `Git` → `Pull`（或工具栏蓝色向下箭头 ↓）
3. 确认远程分支 → 点击 `Pull`

## 方式二：命令行

```bash
cd E:/VSCode_Project/TextMamba3D && git pull
```

## 方式三：Claude Code

直接告诉 Claude：`帮我 pull TextMamba3D`
